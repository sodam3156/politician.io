# 보안

- 인증키는 브라우저가 아니라 로컬 수집 프로세스에서만 사용합니다.
- 사용자 추적 목록은 해당 브라우저에 저장됩니다. 공용 기기 사용 후 브라우저 사이트 데이터를 정리하세요.
- 비밀값이나 개인정보를 공개 이슈, PR, 스크린샷에 올리지 마세요.
- 키가 노출됐다면 발급처에서 폐기·재발급하고 관련 접근 기록을 확인하세요. Git 파일 삭제만으로 기존 이력에서 사라지지 않습니다.
- GitHub에 비공개 취약점 신고 기능이 표시되는 경우 그 경로를 사용하세요. 비공개 연락 경로가 확인되지 않으면 먼저 재현 비밀값 없이 보안 연락 경로를 요청하세요.

이 프로토타입에는 로그인·결제·원격 사용자 데이터 저장 기능이 없습니다. 새로 추가할 때 별도의 위협 모델과 데이터 보존 정책을 검토해야 합니다.

## 로컬 API 연결 도우미

연결 도우미는 127.0.0.1:8771에서만 수신하고 외부 Origin·Host 요청을 거부합니다.
키는 Windows DPAPI 보관함 또는 프로세스 환경변수에서만 읽고, API로 반환하지 않습니다.
인증키·요청어·검색결과 본문은 로그에 남기지 않습니다. 호출량과 성공 여부만 로컬에 보관합니다.
뉴스·영상은 메모리에서만 조회하며 브라우저의 외부 이미지·원문 링크를 열면 해당 제공처의 개인정보 정책이 적용됩니다.
이 도우미를 0.0.0.0에 노출하거나 인터넷 프록시 뒤에 배포하지 마세요. 다중 사용자 운영에는 별도의 인증·권한·비밀 관리 설계가 필요합니다.
